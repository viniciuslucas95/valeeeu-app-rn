import { TextSizeConfig } from '../../../configs';

export const FontSize = {
  big: TextSizeConfig.big,
  medium: TextSizeConfig.medium,
  small: TextSizeConfig.small,
  tiny: TextSizeConfig.tiny,
};
